<Window x:Class="my_tunes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:my_tunes"
        mc:Ignorable="d"
        Title="myTunes by Gus and Spencer" Height="450" Width="800">
    <Grid Margin="0,0,10,-6">

        <ListBox x:Name="playlistListBox" Margin="10,37,677,10" SelectionChanged="playlistListBox_SelectionChanged">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="rename" Header="Rename" Click="rename_Click"/>
                    <MenuItem x:Name="delete" Header="Delete" Click="delete_Click"/>
                </ContextMenu>
            </ListBox.ContextMenu>

            <ListBoxItem>
                <StackPanel>
                    <Label>All Music</Label>
                </StackPanel>
            </ListBoxItem>
            <StackPanel>
                <Label Content="{Binding name}"/>
            </StackPanel>
        </ListBox>
        <DataGrid Name="songsDataGrid" Margin="118,37,10,76" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding title}"  Header="Title"/>
                <DataGridTextColumn Binding="{Binding artist}"  Header="Artist"/>
                <DataGridTextColumn Binding="{Binding album}"  Header="Album"/>
                <DataGridTextColumn Binding="{Binding genre}"  Header="Genre"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="playButton" Content="Play" HorizontalAlignment="Left" Margin="118,370,0,0" VerticalAlignment="Top" Height="40" Width="94"/>
        <Button x:Name="stopButton" Content="Stop" HorizontalAlignment="Left" Margin="217,370,0,0" VerticalAlignment="Top" Height="40" Width="94"/>
    </Grid>
</Window>
